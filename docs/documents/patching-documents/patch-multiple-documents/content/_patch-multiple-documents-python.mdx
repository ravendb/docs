import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Panel from '@site/src/components/Panel';
import ContentFrame from '@site/src/components/ContentFrame';

<Admonition type="note" title="">

* Set-based patch operations apply changes to documents selected by a query,  
  rather than targeting each document individually.

* To patch a single document, see [Patch a Single Document](../../../../documents/patching-documents/patch-a-single-document/api-overview.mdx).  
  You can also patch multiple documents using the [Patch view in Studio](../../../../documents/patching-documents/patch-multiple-documents/patch-view.mdx).

* In this article: 
  * [Overview](#overview)
      * [Defining set-based patching](#defining-set-based-patching)
      * [Important characteristics](#important-characteristics)
  * [Examples](#examples)
      * [Update all documents in a collection](#update-all-documents-in-a-collection)
      * [Change the collection of matching documents](#change-the-collection-of-matching-documents)
      * [Update by dynamic query](#update-by-dynamic-query)
      * [Update by static index query](#update-by-static-index-query)
      * [Update all documents in the database](#update-all-documents-in-the-database)
      * [Update documents by ID](#update-documents-by-id)
      * [Update documents by ID using parameters](#update-documents-by-id-using-parameters)
      * [Increment a counter](#increment-a-counter)
      * [Append a time series entry](#append-a-time-series-entry)
      * [Allow patching based on stale index results](#allow-patching-based-on-stale-index-results)
  * [Syntax](#syntax)  
      * [Send syntax](#send-syntax)  
      * [PatchByQueryOperation syntax](#patchbyqueryoperation-syntax)  

</Admonition>

<Panel heading="Overview">

<Admonition type="note" title="">

#### Defining set-based patching

* In a relational database, a SQL statement that updates a set of rows might look like this:  
  `UPDATE Users SET IsActive = 0 WHERE LastLogin < '2020-01-01'`

* In RavenDB, a `PatchByQueryOperation` combines two components:

  1. **The QUERY**:  
     An [RQL](../../../../querying/rql/what-is-rql.mdx) query that selects the documents to update.  
     The selection portion uses the same RQL syntax used to query collections and indexes.

  2. **The UPDATE**:  
     A JavaScript `update` clause that defines the changes to apply to each selected document.

* When you send the `PatchByQueryOperation`, the server runs the query and executes the `update` clause for each selected document.

<Tabs groupId='languageSyntax'>    
<TabItem value="rql" label="RQL">
```sql
// Query and update example
// Increase Freight for Orders documents that match the query criteria.
// ====================================================================

from Orders
where Freight < 10
update {
    this.Freight += 10;
}
```
</TabItem>
</Tabs>    

</Admonition>
    
<Admonition type="note" title="">

#### Important characteristics

* **Transactional batches**:  
  RavenDB patches the selected documents in batches of up to 1,024.  
  Each batch runs in a separate write transaction, so the operation as a whole is not atomic.

* **Document selection**:  
  Do not rely on documents created after the operation starts being included.

* **Concurrency**:  
  The operation does not hold a snapshot of document contents for its entire duration.  
  RavenDB performs no per-document concurrency checks, so a selected document can be modified or deleted before its batch is processed.  
  If the document still exists, the patch is applied to its current version.

* **Stale indexes**:  
  When the query uses an auto-index or static index, the Python client allows stale results by default,  
  because `QueryOperationOptions.allow_stale` defaults to `True`.  
  The selected set may then omit newly matching documents or include documents based on outdated indexed values.  
  See [Allow patching based on stale index results](../../../../documents/patching-documents/patch-multiple-documents/patch-multiple-documents.mdx#allow-patching-based-on-stale-index-results).

* **Map-reduce indexes**:  
  Set-based patch operations cannot be executed against map-reduce indexes.

* **Long-running operations**:  
  `PatchByQueryOperation` runs as a background server operation and may take a long time to complete.  
  The `send_async` method returns an `Operation` object that you can use to wait for completion.  
  See [Manage lengthy operations](../../../../client-api/operations/what-are-operations.mdx#manage-lengthy-operations).

</Admonition>

</Panel>

<Panel heading="Examples">

<ContentFrame>

### Update all documents in a collection

This query targets every document in the `Orders` collection and does not require an index.

<TabItem value="update_whole_collection" label="update_whole_collection">
```python
# Update all documents in the Orders collection
# ==============================================

# Define the query and JavaScript update clause.
patch_by_query_op = PatchByQueryOperation(
    """from Orders as o
       update
       {
           o.Freight += 10;
       }""")

# Send the operation to the server.
operation = store.operations.send_async(patch_by_query_op)

# Wait for the background operation to complete.
operation.wait_for_completion()
```
</TabItem>

</ContentFrame>
<ContentFrame>

### Change the collection of matching documents

Changing a document’s `@collection` metadata requires deleting and recreating the document with the same ID.

<TabItem value="update_collection_name" label="update_collection_name">
```python
# Recreate all Orders documents in the ArchivedOrders collection
# ===============================================================

patch_by_query_op = PatchByQueryOperation(
    """from Orders
       update
       {
           var documentId = id(this);

           del(documentId);
           this["@metadata"]["@collection"] = "ArchivedOrders";
           put(documentId, this);
       }""")

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>
<br/>    

<Admonition type="warning" title="Document extensions are deleted">

Deleting the document also deletes its attachments, counters, and time series.  
When [revisions](../../../../document-extensions/revisions/overview.mdx) are enabled, RavenDB may also create revision entries for the deletion and recreation.

</Admonition>    

</ContentFrame>
<ContentFrame>

### Update by dynamic query

RavenDB evaluates this query using a matching auto-index, creating one if necessary.

<TabItem value="update_by_dynamic_query" label="update_by_dynamic_query">
```python
# Update documents selected by a dynamic query
# ============================================

# Set the discount on every line of each order handled by the specified employee.
patch_by_query_op = PatchByQueryOperation(
    """from Orders as o
       where o.Employee = 'employees/4-A'
       update
       {
           o.Lines.forEach(line => line.Discount = 0.3);
       }""")

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>

</ContentFrame>
<ContentFrame>

### Update by static index query

The static index selects the source documents to patch.  
The `update` clause modifies those documents, not the index entries.

The index must be a map index; set-based patching does not support map-reduce indexes.

<Tabs>
<TabItem value="patch-operation" label="Patch operation">
```python
# Deploy the static index.
Products_BySupplier().execute(store)

index_query = IndexQuery(
    query="""from index 'Products/BySupplier' as p
             where p.Supplier = 'suppliers/12-A'
             update
             {
                 p.Supplier = 'suppliers/13-A';
             }""")

options = QueryOperationOptions(
    # Reject stale index results, and
    # wait for the index to become non-stale.
    allow_stale=False,
    stale_timeout=datetime.timedelta(seconds=30)
)

patch_by_query_op = PatchByQueryOperation(index_query, options)

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>

<TabItem value="index-definition" label="Index definition">
```python
class Products_BySupplier(AbstractIndexCreationTask):
    def __init__(self):
        super().__init__()
        self.map = "from product in docs.Products select new { Supplier = product.Supplier }"
```
</TabItem>
</Tabs>

</ContentFrame>
<ContentFrame>

### Update all documents in the database

Use `@all_docs` to select documents from every collection without requiring an index.  
Use this scope carefully because the same patch is applied across all collections.

<TabItem value="update_all_documents" label="update_all_documents">
```python
# Update documents across all collections
# =======================================

patch_by_query_op = PatchByQueryOperation(
    """from @all_docs
       update
       {
           this.Updated = true;
       }""")

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>

</ContentFrame>
<ContentFrame>

### Update documents by ID

Use `@all_docs` to select documents by ID across different collections.  
This query does not require an index, and IDs that do not exist are not patched.

<TabItem value="update_by_id" label="update_by_id">
```python
# Update documents with the specified IDs
# =======================================

patch_by_query_op = PatchByQueryOperation(
    """from @all_docs as d
       where id(d) in ('orders/1-A', 'companies/1-A')
       update
       {
           d.Updated = true;
       }""")

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>

</ContentFrame>
<ContentFrame>

### Update documents by ID using parameters

Use `query_parameters` to pass the document IDs without embedding them in the RQL string.  
The ID-based query does not require an index.

<TabItem value="update_by_id_using_parameters" label="update_by_id_using_parameters">
```python
index_query = IndexQuery(
    query="""from @all_docs as d
             where id(d) in ($ids)
             update
             {
                 d.Updated = true;
             }""")

index_query.query_parameters = {
    "ids": ["orders/830-A", "companies/91-A"]
}

patch_by_query_op = PatchByQueryOperation(index_query)

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>

</ContentFrame>
<ContentFrame>

### Increment a counter

Use `incrementCounter(document, counterName, incrementBy)` to increment a counter on every document selected by the query.  
    
You can omit `incrementBy` to increment by `1`.  
If the counter does not exist, RavenDB creates it with the incremented value.

This example uses a direct collection query and does not require an index.

<TabItem value="increment_counter" label="increment_counter">
```python
# Increment a counter on every Product document
# =============================================

query = IndexQuery(
    query="""from Products as p
             update
             {
                 incrementCounter(p, $counterName, $incrementBy);
             }""")

query.query_parameters = {
    "counterName": "PriceUpdates",
    "incrementBy": 1
}

patch_by_query_op = PatchByQueryOperation(query)

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>
<br/>
    
For additional patch-script operations, including retrieving and deleting counters, see [Counter operations](../../../../server/kb/javascript-engine.mdx#counter-operations).

</ContentFrame>
<ContentFrame>

### Append a time series entry

Use `timeseries(document, name).append(timestamp, values)` to append an entry to a time series on every document selected by the query.  
    
`values` must be an array of numbers.  
You can optionally pass a tag as the third argument to `append`.

If the time series does not exist, RavenDB creates it.  
Time series timestamps supplied to patch scripts are treated as UTC.

This example uses a direct collection query and does not require an index.

<TabItem value="append_time_series_entry" label="append_time_series_entry">
```python
# Record the current product price in a time series
# =================================================

query = IndexQuery(
    query="""from Products as p
             update
             {
                 timeseries(p, $timeSeriesName)
                     .append($timestamp, [p.PricePerUnit]);
             }""")

query.query_parameters = {
    "timeSeriesName": "PriceHistory",
    "timestamp": datetime.datetime.utcnow().isoformat()
}

patch_by_query_op = PatchByQueryOperation(query)

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>
<br/>
    
For additional patch-script operations, including retrieving and deleting time series data,  
see [Patch time series data - multiple documents](../../../../document-extensions/timeseries/client-api/operations/patch.mdx#patch-time-series-data---multiple-documents).

</ContentFrame>    
<ContentFrame>

### Allow patching based on stale index results

In the Python client, `QueryOperationOptions.allow_stale` defaults to `True`,  
so a set-based patch operation runs even when its auto-index or static index is stale.

Keep `allow_stale` as `True` only when you accept selection based on the index’s current,  
potentially incomplete or outdated results.  
A stale index can omit newly matching documents or select documents that no longer match.

The patch is applied to each selected document’s current version.
When necessary, recheck the condition inside the `update` clause to avoid modifying documents that no longer match.

<TabItem value="update_stale_results" label="update_stale_results">
```python
query = IndexQuery(
    query="""from Orders as o
             where o.Company = $currentCompany
             update
             {
                 // Recheck the current document value because the index result may be stale.
                 if (o.Company === $currentCompany)
                 {
                     o.Company = $newCompany;
                 }
             }""")

query.query_parameters = {
    "currentCompany": "companies/12-A",
    "newCompany": "companies/13-A"
}

options = QueryOperationOptions(
    allow_stale=True
)

patch_by_query_op = PatchByQueryOperation(query, options)

operation = store.operations.send_async(patch_by_query_op)
operation.wait_for_completion()
```
</TabItem>
<br/>

To reject stale index results instead, pass `allow_stale=False`.  
Add `stale_timeout` to wait for the index to become non-stale before the patch runs,  
as shown in [Update by static index query](../../../../documents/patching-documents/patch-multiple-documents/patch-multiple-documents.mdx#update-by-static-index-query).

</ContentFrame>

</Panel>

<Panel heading="Syntax">
    
#### Send syntax

```python
def send_async(self, operation: IOperation[OperationIdResult]) -> Operation: ...
```
<br/>

| Parameter       | Type                              | Description                                                                 |
|-----------------|-----------------------------------|-----------------------------------------------------------------------------|
| **operation**   | `IOperation[OperationIdResult]`   | The operation to execute. For the examples in this article, pass a `PatchByQueryOperation`. |

| Return value | Description |
|--------------|-------------|
| `Operation`  | A handle used to monitor the server-side operation and wait for its completion. |

<Admonition type="note" title="Use send_async, not send">

`store.operations.send` returns the raw `OperationIdResult` and gives you no way to wait for the patch to finish.  
Use `store.operations.send_async` to obtain the `Operation` handle. Despite its name, the method is not a coroutine.

</Admonition>

#### PatchByQueryOperation syntax

```python
# Available constructors:
def __init__(self, query_to_update: str): ...

def __init__(
    self,
    query_to_update: Union[IndexQuery, str],
    options: Optional[QueryOperationOptions] = None
): ...
```
<br/>

| Parameter            | Type                     | Description |
|----------------------|--------------------------|-------------|
| **query_to_update**  | `str`                    | The complete RQL patch statement, containing the query that selects documents and the `update` clause with the JavaScript patching code. |
| **query_to_update**  | `IndexQuery`             | An object containing the RQL patch statement and, optionally, query parameters. |
| **options**          | `QueryOperationOptions`  | Options that control how the patch operation is executed. |

#### Relevant IndexQuery properties

```python
# Relevant properties inherited by IndexQuery:
class IndexQuery(IndexQueryWithParameters[Parameters]):
    query: Optional[str]
    query_parameters: Optional[Parameters]
```
<br/>

| Property             | Description |
|----------------------|-------------|
| **query**            | The complete RQL patch statement. |
| **query_parameters** | Values for parameters referenced in the RQL statement. |

#### QueryOperationOptions properties

```python
class QueryOperationOptions:
    def __init__(
        self,
        allow_stale: bool = True,
        stale_timeout: datetime.timedelta = None,
        max_ops_per_sec: int = None,
        retrieve_details: bool = False,
    ): ...
```
<br/>                    

| Property             | Description |
|----------------------|-------------|
| **allow_stale**      | Determines whether an index-backed patch may operate on stale index results. The default is `True`. |
| **stale_timeout**    | When `allow_stale` is `False`, specifies how long to wait for a stale index to become non-stale. An exception is thrown if the timeout is exceeded. |
| **max_ops_per_sec**  | Limits the number of matched documents processed per second. When specified, the value must be greater than zero. |
| **retrieve_details** | Determines whether per-document operation details are requested from the server. The default is `False`. |

</Panel>
